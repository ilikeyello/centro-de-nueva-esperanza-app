/**
 * Deprecated. The app migrated off YouTube to Mux. Video playback now uses the
 * <mux-player> web component via components/MuxPlayer.tsx. This file is kept as
 * an empty stub so any stale imports don't break the build.
 */
export {};
